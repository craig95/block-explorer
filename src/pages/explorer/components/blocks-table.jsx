import React from 'react';
import styled from 'styled-components';

const PageTitle = styled.h1`
    font-size: 1.5rem;
    font-weight: 600;
`;

const BlocksTable = () => <PageTitle>Latest blocks</PageTitle>;

export default BlocksTable;
